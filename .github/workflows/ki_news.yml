name: KI News
on:
  schedule:
    - cron: '0 5,10,15 * * *'  # 7/12/17 Uhr Berlin (UTC+2)
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
<<<<<<< Updated upstream
=======
      - run: pip install -r requirements.txt  # falls nötig
>>>>>>> Stashed changes
      - run: python ki_news.py
        env:
          OPENROUTER_KEY: ${{ secrets.OPENROUTER_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
<<<<<<< Updated upstream
          publish_dir: .
 
=======
          publish_dir: .  # oder wo die HTML liegt
>>>>>>> Stashed changes
